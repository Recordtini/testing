import { UtilsConfig, Utils } from '../../types';

/**
 * Initializes utility functions for the application
 * @param config - Configuration object
 * @returns Utility functions
 */
declare function init(config: UtilsConfig): Utils;

export = init;
